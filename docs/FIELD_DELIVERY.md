# FIELD V1 交付与运行

2026-09-16 · U23/U25 · 工程已实现，外部审查的两项缺陷已复现修复并另冻结003运行；真实前瞻验收在今日交易时段完成前保持待验收。详见[复核与修复](FIELD_PREOPEN_REVIEW_FIX.md)。

**交付目标是连续判断及其对错记录。14:05UTC已开始真实模型、估值和影子执行；首15分钟观察核验已有记录，但存在3个时点输入缺口及全部等待计算降级，完整日验收仍待完成。** 本轮依据更正后的FIELD附件和[最小方案](FIELD_V1_PLAN.md)，不再采用此前蓝图作为施工清单。

## 冻结运行恢复（15:49UTC）

15:48心跳发现采集器已退出，旧健康页停在15:46:51附近。日志为`Regressing clock requires a new epoch`，尚未确定具体时钟回退来源；失败事务回滚，未删除或改写原始事件。核对旧PID退出及锁释放后，按同目录恢复命令启动冻结实现，PID55084。恢复过程先回放已有1,744,078条事件，15:49:35写入RESTART并以新epoch、generation2连接同一本机只读Gateway，15:49:41写入READY，15:49:53核验进程与现货新鲜。

两个账本仍各一次ASSUMED_FILLED，没有新增意图；163次既有预测保留，模型需要在新连续路径重新预热。完整恢复记录位于`var/field-build-2026-09-16/recovery-1548/`。此项为采集恢复，不能等同于有效模型已经恢复。

## 盘中数据服务中断（15:36UTC核验）

15:30UTC起模型退回WARMUP。只读事件抽取确认IB行情服务多次报告2103中断、2104恢复，历史数据服务也报告2105/2106；本地进程及原连接epoch未退出或重建。15:34曾观察到现货和3/3候选报价恢复新鲜，15:36:22再次出现现货不合格、0/3候选可用，因此服务恢复尚不稳定。

截至15:36:22，累计163次预测、2处分钟路径缺口；短期评分281命中、38失配、7未知。模型仅重新积累1个有效分钟增量，需要按原规则积累20个连续有效增量才能恢复预测。缺失不回填，两账本各一次假设成交不变，不重开意图。事件证据保存在`var/field-build-2026-09-16/data-feed-incident-1530.json`；继续同一冻结运行监控，恢复状态以后续证据为准。

## 首15分钟观察核验（14:20:12UTC快照）

14:05–14:20UTC的30个计划时点已全部记录，27次有效模型均为CONVERGING_CANDIDATE，3次因POST_DEADLINE_PACKET_INPUT保持不可用。三候选全部报价合格覆盖22/30时点，90个候选行中80行报价合格、71行正价值且满足资格。27次有效等待计算全部因数值行动歧义降级，两账本保持各一次假设成交。**窗口已采集核对，不等于所有功能无降级验收通过。**

截至该快照，窗口内预测已有26项1分钟评分：23次区间命中、3次失配，11次靠近Pin、15次远离；19项5分钟评分全部区间命中，但仅9次靠近Pin、10次远离。1分钟点预测MAE为1.2828点，同刻现价基线为1.2881点；5分钟分别为1.6819和1.7521点。样本重叠且只有一个交易日，晚端尚未成熟结果保持右删失。等待评分15项已观察、9项未知，不补为零。

现有收敛标签未在本窗口筛除预测，不能声称已经验证专家所说的收敛门；首次入场的消融结果表明模型价值主要依赖终值集中假设。具体分析和输入证据见[专家提示与真实输出对照](research/FIELD_EXPERT_GATE_COMPARISON_20260916.md)。本次仅更新报告和监控验收标记，冻结运行与既有记录不改写。

## 首次真实业务核验（14:06:35UTC快照）

用户提交的9月16日Pin7608已于13:54:18UTC进入FORECAST事件，原文与图片两个资产均校验。14:05起每30秒发布，截至本快照已有4次真实预测/估值、33根完成分钟、0路径缺口，模型状态READY/PIN/CONVERGING_CANDIDATE；最近一次使用32个连续有效增量，模型80%终值区间7572.61–7639.04。3/3候选真实报价合格。以上区间为本系统模型输出，不是专家原文的50%区间。

两账本均在14:05:00.036279UTC产生一次C7605_W25意图（7580/7605/7630，同日看涨蝶式）；净限价4.8点，估计费用6.13美元。14:05:01.144950UTC后续合格报价满足固定限价，记录ASSUMED_FILLED，估计总成本486.13美元/独立账本。无券商订单；两账本是同日独立反事实，不相加为账户收益。

**首次LOOKAHEAD已明确降级为FIRST_POSITIVE规则。** H=6.3783点；15/31节点C分别6.3890/6.3704，位于H两侧，触发CONTINUATION_DECISION_NUMERICALLY_AMBIGUOUS。正式C保持null，不能称作前瞻等待优于立即入场的验证。后续继续计算，但每日意图已各用一次。

首两项1分钟结果都落在原80%区间内（HIT），点预测误差分别−1.6021、−2.3790点；**两项“向Pin靠拢”的状态方向判断均为false**。区间命中并不代表收敛判断正确。完整FIELD_SCORE已记录这一区分；现有页面短期表只显示区间命中及点误差，尚未展示方向对错列。5分钟与终值评分此时未成熟，不提前下结论。

页面嵌入的模型、价值表与账本已核对一致；真实行情到页面的首批证据在`var/field-review-2026-09-16/first-live-evaluation.json`、`first-live-page-check.json`、`live-continuity-check.json`。这次读取未重启或修改冻结运行。14:05前页面“有效分钟增量0”实际为尚未发布的模型初值，是展示缺陷；采集分钟始终递增。当前页面也仍缺少停止刷新后的过期提示。

## 已实现

- 独立FIELD_PAPER_V1：20–60个当前会话有效分钟增量，带符号及收缩κ、平方增量EWMA、背景/锚定厚尾混合；无有效Pin自动MARKET_ONLY。每30秒当前价格重新条件化，参数陈旧或数据缺口保持未知。
- 解析蝶式期望、真实自然价及估计费用、点EV与0.25点储备，预算资格单列。缺其他结构时保留缺失行，可选合格子集并标明范围。旧DV1的校准和完整候选要求不变。
- 120秒一步等待：同一P混合过程的条件更新；三腿中间价单独识别局部Q尺度，当前自然价差锚定。15/31节点差超过0.05点或行动两侧不一致时明确降级FIRST_POSITIVE。不是全天最优停止。
- LOOKAHEAD与FIRST_POSITIVE共享真实采集流，各最多一次固定净限价意图。1秒延迟、10秒TTL、625美元含费预算。下单协议仍关闭；未成交/未知不伪装成交。
- 中文FIELD.html：状态、模型依据、区间、候选、H/C、压力敏感性、全部原始预测与决策、成熟1/5分钟对错、等待核验、结算链接。事件日志保留原始引用；正式结算后统一评分终值预测和两个反事实账本。

压力项“噪声×1.5”指方差q乘1.5；Pin±5仅在有效Pin存在时计算。厚尾权重不是已估计跳跃概率，80%终值区间是模型分位区间，不是参数的统计置信界。

## 实际取得的历史数据

本机昨日4个运行提取2/17/69/179根分钟记录，保持各自缺口和原路径。IB另取得**2026-08-18至09-15共20个交易日、7,800根SPX分钟bar**，无盘中分钟缺口，19个跨日开盘跳空单列；3个顺序只读请求。资料在本机 `var/field-history-2026-09-16/`，实际取得时刻11:41:45 UTC。这属于今天取得的历史参考，不是历史盲回测或今日实时判断。

历史请求只在单独采集脚本准许精确消息20/25；主Collector白名单未扩。参照[IB历史限制](https://interactivebrokers.github.io/tws-api/historical_limitations.html)。模型只读取同刻波动参照，当前局部参数使用今日连续分钟路径。目标有歧义的8日Pin材料未用于主模型校准。

## 工程证据与限制

85项检查通过：原77项继续通过，U25新增8项覆盖跨分钟包与重复bar、已知现金/未知价值的区分、人工Pin接收/修订/撤回/跨日及旧模式隔离、已有意图不重置和事件标记。原检查覆盖收敛/扩散/无Pin、条件更新、同过程塔式一致性、ENTER/WAIT、固定限价及冻结回放。14:05已出现真实报价驱动的影子执行，具体时点与限制见上方业务核验；它仍不是券商成交或盈利证据。

代码未新增运行依赖。生产计算新增field_model/timing两个模块；field承载记录和单页，field_pin为人工输入小助手，其余复用原运行链。README改为当前入口；STATUS与U21交付注明历史适用范围。没有删除仍用于OBSERVE/DV1或历史回放的代码、冻结快照及证据。

浏览器URL安全策略阻止本地FIELD页面导航，故只完成静态页面核对，**视觉验收未完成**。未通过其他浏览器/HTTP转发绕过该限制。

## 运行与恢复

部署记录：11:56UTC启动001，修正H展示口径后冻结002；U25复现分钟带及等待评分缺陷后，002于13:26:37UTC停止，130事件、零预测/意图，冻结回放一致。003于13:33:41UTC（纽约09:33:41）启动。旧001/002保留，不恢复、不计作预测样本。当前FIELD独立目录 `var/2026-09-16/field-v1-003`，client ID 27218；原observe-r2-003继续原冻结运行。修复错过开盘最初约3分42秒，不回填分钟；模型仍需20个真实连续有效增量。10:05起每30秒决策、15:30停止新意图，预测与评分至16:00，16:10停止采集。北京时间对应22:05/次日03:30/04:00/04:10。

从 `/Users/logan/SPXLab` 启动新授权运行的单条命令：

```sh
.venv/bin/python -m spxlab.cli field-shadow --plan examples/field/2026-09-16.json --directory var/2026-09-16/field-new-run
```

新目录自动冻结。实际已部署目录只用恢复命令，不另开目录重试当天意图：

```sh
.venv/bin/python scripts/round2_check.py --directory var/2026-09-16/field-v1-003
.venv/bin/python scripts/round2_start.py --directory var/2026-09-16/field-v1-003
```

启动器加载该运行的implementation快照，排他锁阻止第二个写入者，caffeinate只随该进程存在。恢复创建新epoch，在途意图变为未知，已使用的每日意图不会重开；模型需重新满足连续分钟预热。原始数据缺口不补造。模型自动生产，无需每日编辑model-inbox；用户给出本日Pin及原文后，Codex可通过[field-pin命令](FIELD_PREOPEN_REVIEW_FIX.md#人工pin最短流程)代录，保留原统计标签和真实接收时间。无Pin仍为MARKET_ONLY。今日FOMC事件已在页面及预测中标明“当前模型未专门定价”。

单页在运行目录FIELD.html，紧凑状态在health.json/field-status.json，全部证据在events.sqlite。停止后回放并结算：

```sh
.venv/bin/python -m spxlab.cli replay-frozen --run var/2026-09-16/field-v1-003 --output var/2026-09-16/field-v1-final-proof
.venv/bin/python -m spxlab.cli settle-all --run var/2026-09-16/field-v1-003 --evidence /absolute/path/to/official-evidence.json
```

正式证据沿用[第二轮结算字段](ROUND2_RUNBOOK.md#收盘结算与最终证据)：当日SPXW PM、Cboe/S&P原件、hash、实际取得时刻、复核者；不能用AM SET或最后报价。SPXW到期通常16:00停止，半日13:00，参照[Cboe规格](https://www.cboe.com/tradable-products/sp-500/spx-options/spx-specifications)。统一结算生成SETTLED_REPORT.md和settlement-field-*.json，包含全部终值误差、CRPS、同候选事后选价遗憾、等待和执行/费用分类；其事后指标不是可实现收益。

正常收盘后不重启、不自动进入次日。官方数据未发布时保留PENDING并间隔至少15分钟核验。最终交付需补真实模型数、有效报价覆盖、H/C成功/降级、具体命中和失配样例、意图/成交实况、正式结算或确实缺失说明。

同任务5分钟心跳 `spx-field-v1-9-16` 已创建，正常不刷屏，盘中取得实测记录并在收盘完成核验后停止。旧观察任务的心跳当前暂停，但其冻结采集进程仍在；本轮不会切换它的代码。定时接续需要机器开机及桌面应用运行，见[官方定时任务说明](https://learn.chatgpt.com/docs/automations?surface=app)。
